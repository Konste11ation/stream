name: 1_core_vec_array
cores:
  0: stream-dvfs/inputs/cores/tpu_like.yaml
  1: stream-dvfs/inputs/cores/vec_128.yaml
  2: stream-dvfs/inputs/cores/offchip.yaml
offchip_core_id: 2
core_connectivity:
  - {type: link, cores: [0, 1],  bandwidth: 128}

  # ───────── Shared off-chip bus ─────────
  - type: bus
    cores: [0, 1, 2]     # all chiplets + DRAM ctrl
    bandwidth: 128