name: attention_head
cores:
  0: stream-dvfs/inputs/cores/array_64.yaml
  1: stream-dvfs/inputs/cores/array_64.yaml
  2: stream-dvfs/inputs/cores/array_64.yaml
  3: stream-dvfs/inputs/cores/vec_128.yaml
  4: stream-dvfs/inputs/cores/offchip.yaml
offchip_core_id: 4
core_connectivity:
  # ───────── 2-D mesh links (bandwidth 256 each) ─────────
  - {type: link, cores: [0, 1],  bandwidth: 256}
  - {type: link, cores: [1, 2],  bandwidth: 256}
    # ───── SIMD core ─────
  - {type: link, cores: [0, 3],  bandwidth: 256}
  # Direct link 
  - {type: link, cores: [0, 4],  bandwidth: 256} 
  - {type: link, cores: [1, 4],  bandwidth: 256}
  - {type: link, cores: [2, 4],  bandwidth: 256}
  # - {type: link, cores: [0, 8],  bandwidth: 256}
  # - {type: link, cores: [1, 8],  bandwidth: 256}
  # - {type: link, cores: [2, 8],  bandwidth: 256}
  # - {type: link, cores: [3, 8],  bandwidth: 256}